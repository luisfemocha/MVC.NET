@model IEnumerable<MVC1.Models.sede>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dir)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.telS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dir)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.telS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idS }) |
            @Html.ActionLink("Details", "Details", new { id=item.idS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idS })
        </td>
    </tr>
}

</table>
